#include "lib.h"

uint8_t check_board(Board my_board)
{
        for (int row = 0; row < 9; row++) {
                
        }

        for (int column = 0; column < 9; column++) {
                
        }

        for (int square = 0; square < 9; square++) {
                
        }
}